# Deeplint

Deeplint is a tool for analyzing and linting deep learning models.

## Requirements

- Python >= 3.10 (Due to newer type hinting compatibility and other key features)

## Installation

```bash
pip install deeplint
```

## Usage

```bash
deeplint analyze --model your_model_file.onnx
```